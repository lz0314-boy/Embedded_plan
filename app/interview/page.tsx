import { InterviewConfig } from "@/components/interview/interview-config";

export default function InterviewPage() {
  return <><div className="eyebrow">模拟面试</div><h1>用回答暴露薄弱点</h1><p className="muted">先回答，再看当前内容草稿和自评维度。这里不生成你的个人经历，也不把录音上传。</p><InterviewConfig /></>;
}
