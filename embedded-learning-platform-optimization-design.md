# 嵌入式软件系统化学习平台优化设计文档

> 文档版本：v2.8
> 编写日期：2026-09-11
> 文档性质：面向个人学习的优化设计基线；只描述设计，不代表代码、内容核验或硬件实验已经完成
> 上位文档：`embedded-learning-platform-design.md`
> 研究附件：`embedded-learning-platform-research.md`

## 0. 设计结论与范围

当前平台最需要解决的是两个问题：内容访问被“待核验”阻塞，内容主要由连续文字组成且缺少关系、步骤和验证入口；同时知识方向没有形成清晰主线。本方案围绕这两个问题优化，目标是让个人可以直接阅读、理解、回忆和复习。

本版保留的核心决定：

1. 非废弃内容可直接学习；`draft`、`reviewed`、`verified` 表示内容成熟度和证据覆盖，不是访问权限。
2. `start.txt` 只表示一次 eMMC 本地启动日志；`start..txt` 当前不存在，不能作为 TFTP/NFS 真实日志来源。
3. 内容按 C99 -> Cortex-M -> RT-Thread -> Linux 用户态 -> Linux BSP 组织，ESP32 保持独立分支。
4. 课程增加流程图、时序图、状态图、核心代码、日志、调试步骤和主动回忆入口；图示失败时必须有文字说明。
5. IndexedDB 是个人学习的本地数据来源；网络、登录和 Supabase 不得成为阅读、做题或保存进度的前置条件。
6. C99、RT-Thread 版本、具体 MCU、i.MX6ULL SoC 和 ALPHA 板级事实分开记录，不用一个徽标覆盖全部边界。

### 0.1 本次范围取舍

| 项目 | 决定 | 理由 |
|---|---|---|
| JSON 备份 | 保留用户主动导出的轻量快照 | 防止浏览器清理或换设备造成学习数据丢失，复杂度可控 |
| 录音 | 从本方案删除 | 单纯保存音频不能形成学习闭环；面试先做文字回答、计时、自评、追问和复盘 |
| AI 模拟面试 | 不作为本轮必做 | 需要另行定义模型、费用、隐私和评价规则，不能用录音替代 |
| Supabase 同步 | 未来可选 | 单设备个人学习不需要账号和云端；当前不为同步增加使用门槛 |
| 项目经历模块 | 未来可选 | 只有用户提供真实经历后才有内容价值，不影响知识学习主线 |
| 内容建设 | 分批交付 | 先打通一小条可用路线，再根据学习反馈扩展协议、低功耗、AI 和 ESP32 |

本版不把维护者的逐条核验、账号同步、真板实验或完整事件重放写成当前学习前置条件。它们只作为后续维护或扩展边界。

## 1. 产品目标

### 1.1 目标用户

个人嵌入式软件学习者。用户可能接触过 STM32、ESP32、GD32 或 Linux 项目，但需要把零散经验整理成可解释、可复习、可用于面试和调试的知识体系。

### 1.2 当前学习闭环

打开平台后，用户应能完成：

1. 看到今天的复习任务、预计时长和下一项新主题。
2. 用结论、范围、图示、代码或日志理解一个主题。
3. 完成一个小测、自评或主动回忆，知道自己是否掌握。
4. 记录进度、笔记、收藏、错题和复习状态，并在刷新或关闭后继续。
5. 在无网络、未登录或 Supabase 不可用时照常学习。

### 1.3 非目标

本轮不做服务器端运行时渲染、不把 Supabase 作为公开内容数据库、不模拟真实 MCU/RTOS/板卡、不批量生成无来源文章、不编造个人经历，也不加入仅保存音频的功能。

## 2. 内容模型与证据边界

### 2.1 内容角色与生命周期

`contentRole` 和 `status` 分工如下：

| 字段 | 值 | 含义 |
|---|---|---|
| `contentRole` | `core` | 核心路线内容，结构完整后可进入默认计划 |
|  | `supporting` | 补充内容，可搜索、阅读和手动加入计划 |
|  | `placeholder` | 导航或待建设条目，可查看但不进入默认计划 |
| `status` | `draft` | 有学习价值但仍在编辑或证据补充中 |
|  | `reviewed` | 结构、范围和主要来源已人工检查 |
|  | `verified` | 声明范围内的关键结论已有可追溯核验 |
|  | `deprecated` | 历史内容，默认不加入新计划 |

旧内容迁移时不能按标题、文件名前缀或数量自动推断 `contentRole`。无法判断的条目进入 `placeholder` 或人工清单，并在构建报告列出。

默认计划只选择 `contentRole: core`、`status != deprecated`、前置条件满足且核心结构完整的内容。`draft` 可以学习，也可以在满足结构和证据边界后进入计划；`verified` 不是进入计划的充分条件。

### 2.2 证据表达

证据附着于具体事实，不用一个页面徽标覆盖所有句子。涉及标准、架构、固定版本、具体芯片、板级参数、日志观察或推断时，至少记录事实类型、适用范围和证据状态：

| 类型 | 表达方式 |
|---|---|
| `standard-fact` | 标准规定的语义，注明标准文本 |
| `architecture-fact` | Cortex-M、Cortex-A7 等架构定义，不能扩展到具体芯片映射 |
| `implementation-fact` | 绑定 RT-Thread、Linux、U-Boot 的版本或 commit |
| `chip-fact` | 绑定完整 MCU 料号和官方手册 |
| `board-fact` | 绑定 ALPHA 板卡版本和匹配资料；无资料写“待板级核验” |
| `observed-log` | 只描述 `start.txt` 本次日志出现的字符串、数值和顺序 |
| `inference` | 明确写“推测/可能”，列出支撑证据 |
| `unknown` | 写明尚缺证据和下一步核验动作 |

`sourceIds` 可以为空，但只有在存在可审计的私有证据引用或明确标注为 `observed-log`/`unknown` 时才允许。私有路径、原始 PDF、完整日志和用户个人数据不进入公开内容。公开课程只保留脱敏片段和安全的来源名称。

### 2.3 可学习性不等于已核验

页面应同时显示学习入口、内容状态和局部证据边界：

> 这份内容可以直接用于学习。状态表示编辑和证据覆盖程度；“待板级核验”只适用于标注的板级事实。

不得因 `status !== verified` 或 `verifiedAt === null` 隐藏正文、禁止做题或强迫用户逐条确认。阅读完成只更新进度；FSRS 只有在主动回忆、自评或有明确评分规则的测验动作后才更新。

## 3. 课程与展示规范

### 3.1 每篇核心课程模板

每篇核心课程至少包含：

1. 一句话结论、适用范围和不适用范围。
2. 前置知识和学习目标。
3. 流程图、时序图、状态图、对比表或等价文字步骤。
4. 10--60 行左右的核心代码、伪代码或日志片段。
5. 调试/验证方法、反例和限制。
6. 30 秒回答、展开回答、至少两个递进追问。
7. 章节自测、主动回忆和关联题目。
8. 来源、版本、核验日期和证据边界。

“至少一张图”是为了表达关系，不要求复杂美术插图。Mermaid 失败时必须保留与图一致的文字步骤。

### 3.2 Markdown/MDX

第一版固定为构建期受控解析：Markdown/GFM -> frontmatter 校验 -> AST/MDX 编译 -> 受控组件 -> 静态页面。只允许明确白名单组件；拒绝任意 JSX、`import`/`export`、表达式、原始 HTML、事件属性、脚本和 `javascript:`/`data:` 链接。构建失败必须报告文件、行号和原因，不得生成空正文或静默沿用旧内容。

支持标题锚点、列表、任务列表、表格、引用、行内代码、fenced code block、图片替代文本和 Mermaid 静态图。手机宽度下代码、表格和图示可读或可横向滚动；图示资源不在浏览器运行时读取仓库文件。

### 3.3 代码和日志

代码片段必须注明工具链、目标平台、编译命令、运行方式、验证状态和限制。`not-run`、`static-reviewed`、`host-compiled`、`target-compiled`、`hardware-tested` 不能混用；浏览器 runner 结果不代表 Cortex-M、RT-Thread 或 i.MX6ULL 真板验证。

## 4. 学习计划与本地数据

### 4.1 30/60/90 分钟计划

计划按稳定规则生成，不随机填充占位内容，不超过预算：

| 计划 | 建议结构 |
|---|---|
| 30 分钟 | 复习约 10 + 核心内容约 15 + 自测约 5 |
| 60 分钟 | 复习约 15 + 新主题约 30 + 测验约 10 + 总结约 5 |
| 90 分钟 | 复习约 20 + 新主题约 40 + 调试/源码约 20 + 总结约 10 |

表中的分钟数是目标分配，不是必须填满的固定配额。到期复习按到期时间、当前重点和稳定内容 ID 排序；同一天同一内容只出现一次。任务预计分钟数之和不得超过预算。某一类任务不足时，剩余时间按“复习 -> 新主题 -> 自测/调试”的顺序重新分配；没有合适任务时显示空状态，不能自动塞入占位内容。课程的完整估计时长不直接重复计入计划，计划保存实际使用的 `plannedMinutes`。

FSRS 评分规则固定为：阅读完成不产生评分；主动回忆或简答自评由用户选择，完全不会或答错为 `Again`，勉强想起为 `Hard`，正常答出为 `Good`，无需思考即可答出为 `Easy`。客观题答对/答错只作为反馈，答题后仍要求用户确认记忆难度，不自动把“答对”写成 `Good`。

### 4.2 本地数据原则

IndexedDB/Dexie 保存设置、进度、笔记、收藏、答题、错题、复习卡片和计划快照。事件可以用于进度记录和未来同步，但阶段 0/1 不要求复杂事件溯源或跨设备重放。后续迁移必须保留旧数据和稳定 ID，失败时进入恢复提示，禁止清库重试。

### 4.3 JSON 备份

备份是用户主动导出的当前数据快照，格式为 `embedded-learning-backup`，建议包含 `schemaVersion`、`exportedAt`、单篇内容版本摘要、`checksum` 和 `data`。`data` 至少包括设置、进度、笔记、收藏、答题、错题和复习数据，必要时包括文字面试会话和代码草稿。`contentVersion` 表示单篇内容的规范化版本摘要；正式发布清单另记录 `releaseCommit`，两者不能混用。

备份不包含音频、同步游标或待上传队列。导入先解析、校验和预览，再合并；替换本机数据前自动导出当前快照。任何校验失败都不得部分写入。备份不是事件日志，也不承诺重放全部历史操作；只有字段实际变化时才递增 `schemaVersion`。

### 4.4 面试功能

当前只设计文字回答、计时、自评、追问和复盘。AI 模拟面试需要模型、提示词、评价标准、隐私和费用方案，另立需求后再评估；在此之前不保留录音表、录音导出、录音同步或语音验收项。


## 5. 核心学习路线

### 5.1 主线顺序

核心路线固定为：

```text
C 语言与内存模型
  -> 编译、链接、启动文件和调试基础
  -> Cortex-M 通用架构
  -> MCU 外设、中断、DMA 和故障定位
    -> RT-Thread 主线：通用机制标签 -> API -> 内核源码 -> PendSV 汇编
  -> Linux 用户态系统编程与调试
  -> i.MX6ULL / Linux BSP 启动链
  -> 设备树、驱动模型、构建系统和板级调试
```

Linux 用户态与 Linux BSP 共享 C 和操作系统基础，但两条路线不应合并成一篇“大而全”的文章。

### 5.2 分层规则

| 层级 | 讲什么 | 不能越界成什么 |
|---|---|---|
| `rtos-generic` | RT-Thread 课程中的通用概念层：实时性、调度、同步、阻塞、优先级反转 | 页面不单独展示为另一条 RTOS 学习路线 |
| `rt-thread` | 同一条主线中的 RT-Thread 对象、调度器、IPC、时钟、内存和 FinSH 实现 | 具体实现结论必须绑定 RT-Thread 版本 |
| `cortex-m` | Cortex-M3/M4 异常、NVIC、SysTick、PendSV、栈帧和上下文 | 不能当作所有 ARM 或 ESP32 的行为 |
| `chip` | STM32、GD32 或其他具体 MCU 的寄存器、时钟和外设 | 必须带具体系列/料号和官方资料 |
| `esp32` | 具体 ESP32 架构、ESP-IDF 和其运行时模型 | 不能归入 Cortex-M3/M4 |
| `linux-user` | Linux/POSIX 用户态、进程、线程、I/O、IPC、网络和调试工具 | 不能伪装为内核或 BSP 行为 |
| `linux-bsp` | i.MX6ULL、U-Boot、Linux 内核、设备树、根文件系统和构建系统 | 不能把板级参数外推为 SoC 固有行为 |
| `alpha-board` | 正点原子 ALPHA V2.4 的板级器件、介质、脚本和实验 | 没有匹配证据时必须标记待板级核验 |

目标 schema 的 `scope` 允许值固定为：`rtos-generic`、`rt-thread`、`cortex-m`、`chip`、`esp32`、`linux-user`、`linux-bsp`、`alpha-board`。其中 `rtos-generic` 仅用于检索和页面内分层，不在知识地图中生成独立的 RTOS 路线节点。

### 5.3 ESP32 独立分支

ESP32 内容单独进入 `esp32` 主题分支：

- 说明具体芯片使用的 Xtensa 或 RISC-V 架构。
- 标记具体 ESP-IDF 版本。
- 单独讲解其启动、任务模型、外设驱动和调试工具。
- 不使用 Cortex-M 的异常号、MSP/PSP、PendSV 或 NVIC 结论解释 ESP32。
- 如果需要比较 RTOS，只做概念或生态对比，不能把 RT-Thread 课程内容复制到 ESP32。

### 5.4 版本矩阵与断言边界

内容发布前必须把“学习基线”和“实际验证对象”写清楚。版本变化会使受影响内容重新进入评审，不允许只更新页面日期。

| 学习层 | 当前默认基线 | 可以直接说明 | 不能直接外推 |
|---|---|---|---|
| C 语言 | C99；公开参考文本使用 WG14 `N1256`（C99/TC3 公开参考文本） | 标准语义、未定义/未指定/实现定义行为 | 把 GCC 扩展或 C11 特性说成 C99 |
| RT-Thread | 固定教学基线 `v5.2.2`，对应 commit `ddf52e2cdd977f14fc04035c88672ac204aec713` | 该 commit 和声明配置下的对象、调度、IPC、时钟和端口实现 | “当前 RT-Thread”或其他版本的源码结构 |
| Cortex-M3 | 架构手册 + `STM32F103C8T6` 芯片资料 | 架构通用行为和该完整料号的芯片实现 | 其他 STM32F1 料号的存储容量、引脚和启动配置 |
| Cortex-M4 | 架构手册 + `STM32F411` 系列资料 | Cortex-M4/M4F 通用行为和已明确料号的差异 | 在完整料号、开发板和资料未确定前写具体引脚、Flash 容量或下载命令 |
| i.MX6ULL 日志案例 | 用户提供的 `start.txt`；日志显示 U-Boot `2016.03-g0ae7e33`、Linux `4.1.15-ge48931b1` | 这一次启动中的字符串、数值、顺序和结果 | 把旧 vendor kernel/U-Boot 行为说成当前主线 Linux 或所有 ALPHA V2.4 |
| ALPHA 板级 | 具体板卡版本、教程版本、镜像和原理图必须匹配 | 匹配资料能证明的器件、接线、脚本和参数 | 从通用入口、相邻版本或一份日志猜测偏移/分区/DTS |
| ESP32 | 具体型号和 ESP-IDF 版本另行声明 | 该型号和版本的架构、启动和任务模型 | 将 Xtensa/RISC-V 结论套到 Cortex-M |

`N1256` 是公开的 C99/TC3 参考文本，不等于某个编译器在所有目标 ABI 上的完整行为保证。标准语义使用该参考文本，编译器诊断和扩展使用 GCC/目标工具链资料；现有 `N1570` 只能作为 C11 对比来源，不能继续被内容声明为 C99 默认来源。RT-Thread `v5.2.2` 是固定教学基线而不是“当前版本”声明；来源登记必须同时保留 tag、commit、访问日期和所分析的配置宏，若 tag 与 commit 不一致则以固定 commit 为准并阻止发布。

调试、协议、架构、低功耗和 AI 辅助编程属于跨支柱能力，不新增 `pillar` 值。第一版继续使用已有五个支柱，通过 `module`、`keywords`、`scope` 和关联 ID 表达交叉关系，避免内容 schema 与知识地图出现两个分类系统。

## 6. 内容支柱与优先顺序

### 6.1 第一优先级：核心路线

第一批重写不追求数量，优先让以下主题形成连续路线：

#### C 语言与工具链

- 以 C99 作为课程基线；默认命令使用 `-std=c99`。
- `content/sources.yml` 当前的 `c-wg14-n1570` 是 C11 参考，不能继续作为 C99 默认来源；阶段 0 必须新增 C99 的 `N1256` 公开号文本参考，并迁移受影响内容。
- 区分标准 C99、实现定义行为、未定义行为、GCC 扩展和目标 ABI。
- 代码练习默认使用 `-Wall -Wextra -Wpedantic`，需要讨论警告升级时再使用 `-Werror`。

- 对象、类型、整数提升和表达式求值。
- 指针、数组、字符串和函数指针。
- `const`、`volatile`、`static`、存储期、链接和作用域。
- 栈、堆、对齐、大小端、未定义行为。
- 预处理、编译、汇编、链接、ELF、符号和 Map 文件。
- GDB、编译警告和最小故障定位流程。

#### Cortex-M 与 MCU

- 内核寄存器、特权级、MSP/PSP 和内存映射。
- 启动文件、向量表、复位、异常、NVIC、SysTick 和 PendSV。
- 中断上下文、栈帧、HardFault 诊断。
- GPIO、定时器、UART、SPI、I2C、DMA 的驱动思路。
- STM32/GD32 的共性与具体系列差异。

#### RT-Thread 主线：从通用机制到内核实现

- 实时性、延迟、抖动、优先级和确定性。
- 线程状态、就绪队列、调度点和上下文切换。
- 信号量、互斥量、事件、邮箱、消息队列和等待队列。
- 优先级反转、死锁、临界区和 ISR 限制。
- RT-Thread 对象模型、公共调度代码、UP 端口和 Cortex-M 上下文切换。
- 软件定时器、内存分配器、栈评估和故障定位。
- 从零实现最小 RTOS：任务控制块、就绪集合、最高优先级选择、延时队列、时间片和空闲任务。
- 逐步加入信号量、互斥锁、消息队列、邮箱和事件标志组，比较同步、传输数据和等待条件的差异。
- 用 C99 主机模拟验证算法，再映射到 Cortex-M 的 SysTick/PendSV 上下文切换。
- 以 RT-Thread `v5.2.2`（官方 tag 对应 commit `ddf52e2cdd977f14fc04035c88672ac204aec713`）为对照，解释哪些是通用设计、哪些是具体源码和配置行为。该版本映射必须在来源登记中保留 tag、commit 和访问日期，不能用“当前版本”替代。

#### Linux 用户态

- 文件 I/O、阻塞/非阻塞、目录和权限。
- `fork/exec/wait`、线程、同步和信号。
- 管道、共享内存、消息队列、Unix Domain Socket。
- TCP/UDP、`select/poll/epoll` 和协议状态机。
- Make/CMake、交叉编译、GDB、`strace`、`perf` 和日志。
- 进程崩溃、死锁、资源泄漏和性能问题的定位流程。

#### Linux BSP 与 i.MX6ULL

- i.MX6ULL 是 Cortex-A7 应用处理器，不能当作 Cortex-M 讲解。
- 上电、复位、Boot ROM、IVT/DCD、U-Boot、Linux、设备树、根文件系统和 PID 1。
- U-Boot 加载内核和 DTB 的输入、输出与日志。
- Linux 设备树、platform 总线、驱动匹配和 `probe`。
- Buildroot/Yocto、交叉工具链、根文件系统和启动脚本。
- 串口日志、启动失败、设备树、驱动和文件系统问题的排查。

### 6.2 第二优先级：调试能力

调试不作为零散附录，而是贯穿每个支柱：

| 阶段 | 必学调试问题 |
|---|---|
| C | 编译警告、未定义行为、内存越界、GDB 断点和回溯 |
| Cortex-M | HardFault、寄存器、栈帧、Map 文件、Watchpoint、栈溢出 |
| RT-Thread | 线程状态、优先级反转、死锁、IPC 超时、栈余量、日志 |
| Linux 用户态 | 崩溃、core dump、`gdb`、`strace`、`lsof`、`perf`、日志分层 |
| Linux BSP | 串口启动日志、U-Boot 环境、`bootargs`、设备树、`dmesg`、sysfs |

每个调试主题都使用同一个故障定位框架：

```text
现象
  -> 可观测证据
  -> 假设列表
  -> 最小化复现
  -> 加入观测点
  -> 排除假设
  -> 修复
  -> 回归验证
```

课程不能只写“检查日志、检查指针”这种泛化建议，必须给出症状、日志/寄存器/状态、判断依据和下一步行动。

### 6.3 第三优先级：题库扩充

题库扩充顺序：

1. 先为核心课程提供少量高质量题目。
2. 为每个课程提供至少一个“原理题”、一个“代码/日志分析题”和一个“调试追问”。
3. 题目必须关联课程、平台、难度和来源边界。
4. 题库数量在核心路线稳定后再扩充，不以批量生成数量作为完成标准。

### 6.4 RT-Thread 从零实现路线

这部分不是要重新实现完整生产级 RT-Thread，而是用最小、可读、可逐步运行的代码建立整体心智模型。所有示例保持短小，完整工程只在实验需要时提供。

#### 第一步：主机上的 C99 调度器模型

先不引入中断和汇编，在主机上实现以下最小结构：

1. 任务控制块：任务状态、优先级、剩余时间片、延时到期时间和栈/上下文占位。
2. 就绪集合：按优先级组织的列表或位图。
3. 选择算法：选择最高优先级就绪任务。
4. 任务状态：运行、就绪、阻塞、延时、挂起和结束。
5. 时间推进：tick 到来后减少延时并唤醒到期任务。
6. 同优先级时间片：只在同优先级存在多个就绪任务时轮转。
7. 空闲任务：没有其他任务就绪时运行。

主机模型用于验证状态转移和调度规则，不宣称模拟真实 Cortex-M 的寄存器、异常或时序。

#### 第二步：IPC 和同步

按“为什么需要、保存什么状态、谁被唤醒、是否传输数据、是否允许超时、ISR 能否调用”的顺序加入：

- 信号量：计数/资源数量或同步事件。
- 互斥锁：所有权、递归使用边界和优先级继承。
- 消息队列：固定大小消息和 FIFO/唤醒规则。
- 邮箱：单字/指针值传递；具体宽度和 API 语义以 RT-Thread 版本及目标架构为准，不当作通用消息队列。
- 事件标志组：多个条件位的组合等待。
- 等待队列：阻塞任务的组织和超时移除。

课程必须明确：信号量不是互斥锁，消息队列/邮箱是数据传输机制，事件标志组表达条件集合；不能只用“都可以阻塞”来回答面试题。

#### 第三步：优先级与时间片设计

任务优先级不按“越高越好”分配，使用以下工程步骤：

1. 先列出周期、最迟响应时间、最坏执行时间、阻塞资源和是否允许丢失事件。
2. 将硬实时/高响应任务、控制任务、通信任务、后台任务和日志任务分层。
3. 高优先级任务必须短、可阻塞边界清晰，不能用高优先级掩盖无界循环或锁竞争。
4. 同优先级时间片只用于公平共享 CPU，不替代事件驱动和截止时间分析。
5. 任务之间通过 IPC 协作，不用忙等循环消耗 CPU。
6. 通过最大阻塞时间、响应延迟、CPU 占用、栈余量和丢事件次数回归验证。

这是一套可解释的工程启发式，不把简单的优先级分配公式包装成适用于所有系统的定理。

#### 第四步：Cortex-M 上的 SysTick/PendSV

最后把主机模型映射到 Cortex-M：

```text
SysTick
  -> tick 加一、处理延时和时间片
  -> 判断是否需要切换
  -> 设置 PendSV 挂起
  -> PendSV 保存当前任务上下文
  -> 选择下一个任务
  -> 恢复下一个任务上下文
  -> 异常返回到线程态
```

汇编讲解至少覆盖：

- 线程态使用 MSP/PSP 的边界。
- Cortex-M 异常进入时自动压入的基本栈帧。
- PendSV 中软件保存和恢复 `R4-R11` 等寄存器。
- TCB 保存的 PSP 和当前任务指针。
- `EXC_RETURN` 如何决定返回线程态和使用哪一个栈。
- Cortex-M4F 启用 FPU 时扩展上下文的额外边界。

最后对照 RT-Thread `v5.2.2` 的 `scheduler_comm.c`、UP（单处理器）调度实现和 Cortex-M `context_gcc.S`，说明教学内核和成熟内核的差异。

上述主机模型是教学用状态机，不等于 RT-Thread 内部的字段枚举，也不等于某个端口的完整调度策略。教材必须在每一步标出“教学抽象”“RT-Thread 公共实现”“Cortex-M 端口”或“芯片/BSP 实现”，避免把模型中的状态名称直接当成 API 或 ABI。

### 6.5 Cortex-M 从上电到 `main()`

课程采用“通用 Cortex-M 链路 + 一个具体 STM32 代表实现”的结构：

```text
上电/复位
  -> 芯片厂商启动模式判断
  -> 芯片在复位启动阶段确定当前向量表映射，或完成芯片级重映射
  -> 硬件从 [vector_base + 0x00] 装载初始 MSP
  -> 硬件从 [vector_base + 0x04] 装载 PC 并进入 Reset_Handler
  -> 按具体启动文件完成栈设置、.data/.bss 和运行时初始化
  -> SystemInit：时钟、FPU/MPU/VTOR 等可选初始化（与 .data/.bss 的先后依启动文件而定）
  -> C 运行时初始化和静态构造（若工具链需要）
  -> main()
  -> 外设/中断/RT-Thread 初始化
```

这里的 `vector_base` 是复位时生效的向量表映射地址，不应固定解释为所有芯片的物理 Flash 地址。向量表第二项是处理函数入口地址；Thumb 架构的入口状态和地址最低位规则必须按 Cortex-M 架构资料解释。`VTOR` 重新定位通常发生在启动代码或系统初始化中，重新定位前后的地址不能混为一谈。

默认具体示例对象：

- Cortex-M3：`STM32F103C8T6`。
- Cortex-M4：`STM32F411` 系列；完整料号和开发板尚未确定时只讲系列/架构边界。
- 工具链：`arm-none-eabi-gcc`、`arm-none-eabi-gdb`；具体调试器和开发板引脚只在对应实验中声明。

必须区分：

- `BOOT0/BOOT1`、Flash/系统存储器/SRAM 启动选择属于具体芯片厂商启动逻辑。
- 向量表首项初始 MSP、次项复位处理函数地址是 Cortex-M 向量表模型。
- `.data`、`.bss`、栈、堆和 `main()` 前的运行时初始化属于启动文件、链接脚本和 C 运行时协作。
- 时钟树、PLL、GPIO 复用、FPU/MPU 配置和 VTOR 地址属于具体 Cortex-M 芯片和工程配置。
- `.data/.bss`、`SystemInit`、`__libc_init_array` 的精确先后顺序依赖启动文件、工具链和库实现；课程必须以实际启动文件为准，不能把某一套 CMSIS 模板顺序写成唯一流程。

用户提到的“上电第四个上升沿锁定 `BOOT0/BOOT1`”不能直接写成 Cortex-M 通用事实。只有在确定具体 MCU 的参考手册明确这样描述时，才作为该型号的专门小节加入；否则课程使用“复位/启动阶段采样启动配置”的通用表述，并在具体 STM32 章节中解释实际采样时机。

中断章节按三层说明：

1. 异常进入时硬件通常自动保存基本栈帧：`R0-R3`、`R12`、`LR`、`PC` 和 `xPSR`；具体扩展帧受架构状态和 FPU 使用情况影响。
2. 中断处理函数或端口代码保存额外寄存器、切换栈或记录故障；PendSV 端口常见地额外保存/恢复 `R4-R11`，但必须以目标端口汇编为准。
3. `EXC_RETURN`、`CONTROL.SPSEL`、MSP/PSP 和异常优先级共同决定返回线程态/处理态及使用的栈。
4. Cortex-M4F 启用 FPU 时还要说明扩展浮点上下文、lazy stacking 和 `EXC_RETURN` 边界，不能把 Cortex-M3 的栈帧图直接套用。

### 6.6 ALPHA EMMC 启动链与真实日志案例

当前工作区可复核的是 `start.txt`，它是一次 eMMC 本地启动日志，原始文件被 `.gitignore` 忽略，不直接提交到公开仓库。`start..txt` 当前不在工作区；此前关于 TFTP/NFS 的分析只能作为待重新取得证据后的教学对照，不能写成当前已核验日志。

`start.txt` 明确支持以下“本次启动观测事实”：

1. U-Boot 为 `2016.03-g0ae7e33`，显示 `Board: I.MX6U ALPHA|MINI`、i.MX6ULL rev1.1、792 MHz（日志同时写明 running at 396 MHz）和 512 MiB DRAM。
2. U-Boot 选择 `mmc1(part 0)`，读取 `zImage` 和 `imx6ull-14x14-emmc-4.3-800x480-c.dtb`，随后输出 `Booting from mmc`。
3. `boot.scr` 读取失败，但后续日志仍显示成功读取内核和 DTB；只能说明本次后续命令继续执行，不能在没有命令回显时断言具体“后备脚本”逻辑。
4. U-Boot 报告环境 CRC 异常并使用默认环境；本次仍然能够启动，不能把该告警等同于内核启动失败。
5. Linux 命令行使用 `root=/dev/mmcblk1p2 rootwait rw`；日志把 SD 卡识别为 `mmcblk0` 并看到 `mmcblk0p1`，把 eMMC 用户区识别为 `mmcblk1` 并看到 `mmcblk1p1/p2`，同时列出精确名称 `mmcblk1boot0`、`mmcblk1boot1` 和 `mmcblk1rpmb`；随后将 `mmcblk1p2` 作为 ext3 根文件系统挂载。
6. 系统最终进入 `root@ATK-IMX6U:~#`，说明本次日志已经走完内核、根文件系统、init 和基本用户空间启动。
7. `FEC1 address not set`、网络服务端口占用、`hostapd` 配置错误、NFS daemon 未启用等属于本次日志中的独立告警，必须与 Boot ROM、U-Boot、内核或 rootfs 的致命故障分层处理。

该日志同时显示 `ALPHA|MINI`、`14x14 EVK` 和基于 V2.4 的驱动文本，不能仅凭这些字符串把所有结论外推为任意 `ALPHA V2.4` 板卡事实。课程将“日志中观察到的事实”和“V2.4 板级资料仍需匹配的参数”分开。尤其不能把日志中的 NFS daemon 报错理解为本次使用了 NFS rootfs。

U-Boot 的 `mmc1`、Linux 控制器日志中的 `mmc1` 和块设备名 `mmcblk1` 属于不同层的编号命名空间。本次日志中它们可以作为关联线索，但不能写成跨平台、跨版本永远相等的规则。

课程分成两条路径：

#### 路径 A：`start.txt` 的 eMMC 本地启动路径

```text
上电
  -> Boot ROM
  -> eMMC 中的启动镜像/U-Boot
  -> U-Boot 选择 mmc1
  -> 从 eMMC 读取 zImage 和 DTB
  -> U-Boot 将内核、DTB 和 bootargs 交给 Linux
  -> Linux 解压和初始化
  -> 内核早期展开 FDT；随后各子系统按 initcall/总线条件匹配和 probe
  -> 存储控制器与文件系统就绪后挂载 /dev/mmcblk1p2
  -> init/启动脚本/应用
```

该流程是教学主序，不表示所有驱动都在根文件系统挂载之前完成 `probe`；根文件系统本身依赖相应存储、块设备和文件系统代码先就绪。U-Boot 是否实际调用 `bootz` 也必须以命令回显、脚本或源码为证，不能仅凭出现 `zImage` 文件名推断。

#### 路径 B：TFTP/NFS 网络启动的教学对照路径

```text
上电
  -> Boot ROM
  -> ALPHA 上的 U-Boot
  -> TFTP 加载 zImage
  -> TFTP 加载 DTB
  -> U-Boot 将内核、DTB 和 bootargs 交给 Linux
  -> Linux 解压和初始化
  -> 内核早期展开 FDT；网络、NFS 和文件系统条件满足后挂载 NFS rootfs
  -> PID 1/启动脚本/应用
```

TFTP/NFS 路径只用于对照网络启动和 rootfs 来源，当前没有 `start..txt` 可复核时不得标记为“真实日志已验证”，也不能覆盖 `start.txt` 的 eMMC 事实。具体 Boot ROM 读取偏移、U-Boot 写入偏移、分区布局和启动脚本，只有在匹配 ALPHA V2.4 官方资料、镜像或脚本后才写入；当前不填猜测值。

设备树讲解必须明确：

- U-Boot 负责获得、搬运、修改或传递 FDT/DTB。
- Linux 内核早期启动阶段解析扁平设备树并建立内核设备树对象。
- 驱动模型根据 `compatible`、总线和资源信息匹配设备并进入 `probe`。
- 设备树描述硬件资源，不是驱动代码，也不是 rootfs。
- 设备树中的节点存在不等于驱动一定绑定成功；必须结合 `compatible`、依赖资源、错误码和日志判断。

rootfs 讲解必须覆盖：

- `/sbin/init`、`/etc`、动态链接器、共享库、设备节点和应用程序。
- `root=` 指定的根文件系统来源。
- NFS rootfs 与 eMMC/SD/ext4 rootfs 的差异。
- 内核启动成功但 PID 1、脚本或应用失败时的日志和定位边界。

### 6.7 GDB 学习路线

GDB 不单独作为工具命令清单，而按目标环境分三层：

| 场景 | 工具链 | 重点 |
|---|---|---|
| 主机 C99 程序 | `gcc` + `gdb` | 断点、单步、变量、栈、watchpoint、信号、core |
| Cortex-M 固件 | `arm-none-eabi-gcc` + `arm-none-eabi-gdb` + 调试器 | Reset_Handler、HardFault、寄存器、栈帧、PendSV、内存和 ELF 符号 |
| ALPHA Linux 用户程序 | 交叉 GCC + `gdbserver` + 主机交叉 GDB | sysroot、动态库、远程进程、线程、core、日志和系统调用 |

每个 GDB 示例必须说明：

- 编译是否带 `-g`。
- 优化等级，例如 `-Og` 或为特定问题使用 `-O0`。
- 目标架构、ABI、sysroot 和符号文件。
- 断点位置和观察目的。
- 如何确认修复，而不是只展示一次成功运行。

Linux BSP 第一阶段先讲用户态 `gdbserver` 和启动链日志分析；内核 kgdb、U-Boot 调试和 JTAG 深入调试作为后续扩展，避免一次引入过多环境依赖。`start.txt` 不证明目标 rootfs 已安装 `gdbserver`；课程必须把“目标端工具是否存在、交叉 GDB 是否有匹配 sysroot、是否有网络/串口调试通道”列为实验前置条件。

### 6.8 常见协议与驱动设计

第一版覆盖经典协议，不把协议页面写成纯定义：

| 协议 | 必讲机制 | 必讲调试问题 |
|---|---|---|
| I2C | 开漏、上拉、地址、ACK/NACK、重复起始、仲裁、时钟拉伸 | 地址错误、无 ACK、总线被拉低、时序和上拉电阻 |
| SPI | 主从、片选、全双工、CPOL/CPHA、位序和吞吐 | 模式不匹配、片选时序、读写方向、DMA 缓冲区 |
| UART | 波特率、帧格式、起始/停止位、校验、接收缓冲 | 波特率错误、乱码、丢字节、环形缓冲和中断延迟 |
| CAN | 总线仲裁、帧、过滤、错误状态、终端电阻和总线负载 | 仲裁丢失、错误帧、Bus-Off、过滤器和物理层 |
| LIN | 单主多从、Break、同步字段、PID、校验和低成本物理层 | 调度表、同步误差、校验类型、收发器和超时 |

每个协议都按“电气层/时序 -> 控制器寄存器或驱动 API -> 缓冲和并发 -> 上层协议状态机 -> 抓波形/日志/回归”组织。CAN FD 暂不作为第一阶段主线。

协议实验不能从“用户拥有 STM32F103C8T6 和 STM32F411”直接推导出硬件能力。阶段 1 先支持主机代码/日志分析和代码审查；真板实验按完整料号、开发板原理图、引脚复用和外部收发器逐项登记：

| 实验对象 | 进入真板实验前必须确认 |
|---|---|
| I2C、SPI、UART | 具体外设实例、引脚复用、电平、上拉/终端和当前板卡连接 |
| CAN | 芯片是否带 CAN 控制器、具体位时序、引脚和外部 CAN 收发器；不能假定 STM32F411 任意料号都具备相同能力 |
| LIN | LIN 控制器或 UART Break 能力、LIN 收发器、主从角色、调度表和校验方式 |

没有示波器/逻辑分析仪、收发器或完整板级资料时，相关内容只能标记为代码审查、波形分析或待实验，不得显示 `hardware-tested`。

### 6.9 架构思维与低功耗

架构课程形成以下可复用框架：

```text
BSP/启动/时钟
  -> HAL 或寄存器适配
  -> 驱动层
  -> 服务层/协议栈
  -> 领域逻辑
  -> 应用任务/状态机
```

重点不是强行套用分层，而是说明：

- 每层拥有的资源和不应该知道的细节。
- 接口、所有权、错误码、超时和并发边界。
- 中断只采集必要信息，复杂处理延后到线程/任务。
- 如何通过依赖注入、mock、日志和故障注入提高可测试性。
- 何时使用状态机、事件驱动、消息传递或共享数据。
- 如何处理升级、配置、故障恢复和向后兼容。

低功耗先以 MCU 为主：

1. 活跃、睡眠、停止、待机等模式和唤醒源。
2. tickless idle、时钟门控、外设关闭和 GPIO 状态。
3. DMA、批量唤醒、事件合并和减少轮询。
4. RAM/寄存器保持、唤醒后恢复和数据一致性。
5. 电流测量、唤醒延迟、功耗预算和回归记录。

Linux 的 CPU idle、runtime PM、系统休眠和设备唤醒作为 i.MX6ULL BSP 的后续扩展，不与 MCU 低功耗概念混成一篇。

低功耗课程不得把某个 STM32 系列的模式名、寄存器位或典型电流值写成 Cortex-M 通用事实。模式、时钟树、唤醒源、RAM 保持和恢复顺序必须绑定完整料号；没有电流计实测时只写测量方法、相对比较和未测量状态，不填通用 mA 数值。看门狗、异常唤醒和恢复失败也属于架构验收的一部分。

### 6.10 AI 辅助编程

AI 章节采用“方法优先、工具可替换”的结构：

1. Prompt 结构：目标、上下文、约束、输入、输出格式、验收标准和禁止事项。
2. 上下文准备：仓库结构、版本、芯片型号、编译器、链接脚本、日志、已知错误和测试命令。
3. Harness：把模型调用包在可重复的流程中，包括前置检查、工具白名单、补丁应用、构建、测试、差异检查和失败报告。
4. MCP 工具：按最小权限暴露文档、源码、构建、测试或设备日志；默认只读，写操作需明确授权，禁止把密钥和私人数据暴露给工具。
5. 嵌入式提示词：必须写清 MCU/SoC、具体料号、架构、工具链、时钟、内存、并发上下文、中断限制和目标行为。
6. 验证闭环：AI 只能起草；来源核对、编译、静态检查、单元测试、真板测试和代码审查由工程流程完成。
7. 调试提示词：先给现象和证据，再要求列出假设、最小复现、观测点和回归测试，禁止让模型凭空猜寄存器地址、启动偏移或个人项目经历。

工具实践默认采用“通用方法 + Codex/MCP 类工具示例”的两层结构，不把某个 AI 产品作为学习网站运行依赖。

### 6.11 快速调试与问题发现

所有调试课程统一使用以下快速路径：

```text
1. 定义现象和通过条件
2. 保存第一次失败证据
3. 判断所属层级
4. 保持变量不变并最小化复现
5. 一次只加入一个观测点
6. 列出并排序假设
7. 用日志、断点、波形、寄存器或系统调用排除假设
8. 用反事实实验确认根因
9. 修复并补充回归测试
10. 更新故障报告和知识卡片
```

快速分层：

- 编译/链接：告警、符号、ABI、链接地址和 Map 文件。
- 启动：复位、向量表、时钟、Boot ROM、U-Boot 和内核入口。
- RT-Thread：线程状态、调度、IPC、锁、超时和栈。
- 协议/外设：电平、时序、帧、缓冲、DMA 和中断。
- Linux/BSP：设备树、probe、rootfs、PID 1、网络、日志和应用。

每个故障条目必须回答“现在知道什么、还不知道什么、下一次实验只改变什么”，避免用大量日志替代判断。


## 7. 内容建设批次与实施顺序

### 7.1 第一批：可用主线

先完成 C99、Cortex-M 启动与异常、RT-Thread 基础调度和 IPC 的纵向样本，打通“课程 -> 图示/代码 -> 自测 -> 进度/复习”。这些内容优先使用主机代码、源码阅读和日志分析，不等待真板。

### 7.2 第二批：主线扩展

在第一批稳定后扩展 RT-Thread 端口、Linux 用户态调试和 i.MX6ULL/ALPHA eMMC 启动链，再加入协议、架构、低功耗和 AI 辅助编程。ESP32 作为独立分支，不能套用 Cortex-M 的异常、NVIC、MSP/PSP 或 PendSV 结论。

### 7.3 内容验收

每个批次检查：frontmatter/schema、课程关联、链接、图示降级、代码元数据、证据边界、移动端阅读和本地学习闭环。内容数量不作为唯一完成标准；占位内容不进入默认计划。

## 8. ALPHA 与 `start.txt` 使用边界

`start.txt` 是一份 eMMC 本地启动日志。可用于讲解本次日志中 U-Boot 读取内核/DTB、Linux 根文件系统和启动告警的观察事实。日志中的 U-Boot/Linux 版本、`mmc1`、`mmcblk1p2`、ext3、DTB 文件名和告警只能按“本次观测”表述，不能外推为所有 ALPHA V2.4 板卡配置。

TFTP/NFS 只保留为通用对照教学路径。当前没有 `start..txt`，因此不能把 TFTP/NFS 写成真实日志已验证，也不能据日志猜测 Boot ROM 偏移、U-Boot 写入偏移、分区表、默认环境、DTS 文件名或升级脚本。板级结论必须绑定完整板卡型号、资料版本、章节/页码或可复核日志定位。

## 9. 可选后续能力

以下内容不属于当前个人学习验收：

- PWA 缓存和离线包的进一步优化。
- Supabase Auth、RLS、跨设备同步和冲突处理。
- 浏览器内受限 C runner、Monaco 和更复杂实验。
- 用户项目经历模板。
- AI 模拟面试、语音或其他音频能力。
- 正式托管、性能、SEO 和多网络发布验证。

启用任何一项前，必须先确认真实需求、数据范围、隐私边界和可执行测试；不得因为原有代码或表结构存在就把它列为当前必做。

## 10. 验收标准

### 10.1 学习体验

- `draft`、`reviewed` 和 `verified` 内容都可学习；只有 `deprecated` 默认不进新计划。
- 课程有结论、范围、图示或文字降级、核心代码/日志、调试步骤、自测和回忆入口。
- C99 -> Cortex-M -> RT-Thread -> Linux 用户态 -> Linux BSP 主线清晰，ESP32 独立。
- `start.txt` 只作为 eMMC 本地启动观测；TFTP/NFS 不伪装成已验证日志。
- 30/60/90 分钟计划不超预算，不选择占位内容；没有任务时显示空状态。
- 阅读完成不自动写入 FSRS `Good`；答题判定按题型规则执行。
- 刷新、关闭浏览器后本地进度、笔记、收藏、答题和复习仍可用。
- JSON 备份可以导出和导入当前个人学习数据；校验失败不部分写入；不包含音频。

### 10.2 内容与安全

- GFM/代码块/表格/引用/图示正确渲染，危险 MDX/HTML/链接被拒绝。
- 每段代码有工具链、目标平台、编译命令和真实验证状态。
- 私人笔记、学习数据、项目经历、原始日志和本机路径不进入公开构建。
- 未核验板级事实明确标注边界，不用数量或状态徽标掩盖证据缺口。
- 构建或内容检查失败返回非零状态，不覆盖上一份可用产物。

### 10.3 当前不验收

本轮不验收 Supabase、Docker/Postgres、跨设备同步、录音/语音、AI 模拟面试、真板下载、示波器测量或正式托管网络表现。它们在有明确需求和环境后另行评审。

## 11. 文档交付状态

本版只修改优化设计文档，未修改应用代码、数据库、内容文件或测试。后续实现应先完成文档与主设计文档的规则同步，再按第一批内容主线实现；所有“已完成”“已验证”“已运行”必须由实际结果支持。
